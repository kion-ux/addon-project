# GRAND LINE AWAKENING — ワンピース統合版アドオン

Minecraft **統合版 (Bedrock Edition)** 用の**非公式ファン制作**アドオンです。
Minecraft・ONE PIECE の公式商品ではなく、権利者とは関係ありません。
公式ロゴ、作品映像、抽出した音声・画像は一切同梱していません。

ルフィの通常形態からギア5（ニカ）までの**変身**と、形態ごとに作り分けた
**24枠の技**、そして変身直後の**浮遊しながらの大爆笑**を中心に作られています。

- 対応バージョン: **Minecraft Bedrock 1.21.30 以降**
- 使用API: `@minecraft/server 1.13.0` / `@minecraft/server-ui 1.2.0`
- 言語: 日本語 / English（ゲームの言語設定に追従）
- namespace: `gla`（他パックと衝突しないよう専用のものを使用）

> **この版で確認できていないこと（重要）**
> 実機での描画・音・タッチ操作・負荷は**まだ検証していません**。
> 下の QA 表で「実機未検証」と書いた項目は、そのとおり未検証です。
> 企画書の方針どおり、確認していないものを「動作確認済み」とは書きません。

---

## 導入方法

1. `dist/GrandLineAwakening_v1.0.0.mcaddon` をインポートする
2. ワールド設定で **ビヘイビアーパック** と **リソースパック** の両方を有効化する
3. ワールド設定の **「ベータAPI」を ON** にする（スクリプト機能に必要）

---

## 操作

タッチ操作を優先し、覚えることを4つに絞っています（企画書 §09）。

| 操作 | 効果 |
|---|---|
| **悪魔の実（ゴム）** を食べる | 能力を得る（最初の一度だけ） |
| **麦わら帽子** を使う | 変身 / 変身中は解除 |
| **しゃがみ＋麦わら帽子** | 形態を選ぶ |
| **拳の包帯** を使う | 選択中の技を1回発動 |
| **しゃがみ＋拳の包帯** | 次の技へ切替（発動とは排他。切替で技は出ません） |
| **ログポース** を使う | 設定・訓練場・通常状態へ復旧 |
| **しゃがみ＋ログポース** | 技の一覧から直接選ぶ |

画面に出しっぱなしにするのは **形態名・選択中の技・気力・再使用待ち** の4つだけ。
詳しい説明はメニュー側にあります（企画書 §12）。

---

## 形態

同じモデルの色替えでは済ませず、輪郭・立ち方・動き・攻撃の性格を形態ごとに
変えています。ギア3だけは常時形態ではなく、**技中だけ前腕と脛が膨張**します。

| 形態 | 解放条件 | 維持コスト | 技数 | 作り分け |
|---|---|---|---|---|
| **通常** | 最初から | なし | 4 | 基本操作・コンボ・移動技の基準。軽快な重心移動と伸びる腕。 |
| **ギア2** | 命中 20 回 | 0.85/秒 | 4 | 前傾姿勢・加速・蒸気。短い予備動作と高速の連続攻撃。 |
| **ギア3** | 命中 60 回 | 0.35/秒 | 4 | 技中だけ腕脚が膨張する。常時形態としては通常の輪郭を保つ。 |
| **ギア4・バウンドマン** | 命中 120 回 | 1.3/秒 | 3 | 胸・腕のボリューム、圧縮される拳、弾む重心、覇気の模様。 |
| **ギア4・スネイクマン** | 命中 200 回 | 1.3/秒 | 3 | バウンドマンとは別の輪郭と構え。長い腕の軌道と方向転換。 |
| **ギア5・ニカ** | 命中 320 回 | 2.1/秒 | 6 | 白い髪の房、眉、歯を見せる大笑い、衣装、帯、雲。弾性の強い動作。 |

解放は「技を当てた回数」で進みます。ログポースの設定からは解放できません
（気力無限を入れても解放条件は勝手に緩みません）。

---

## 技 — 24枠

技名と色を隠して録画を見ても「伸ばす技」「連打」「重い一撃」が区別できること、
を設計の検収条件にしています（企画書 §10）。そのため判定の形・溜めの長さ・
命中回数・ノックバックを意図的にばらしてあります。

| # | 形態 | 技 | 判定 | 溜め / 有効 / 後隙 | 威力 | 気力 | 再使用 |
|---|---|---|---|---|---|---|---|
| 1 | 通常 | **ゴム銃** | 直線（通過した軌道） | 4/6/8t | 7 | 5 | 0.8秒 |
| 2 | 通常 | **ゴム大砲** | 前方の円錐 | 10/6/12t | 11 | 11 | 2.4秒 |
| 3 | 通常 | **ゴム乱打** | 直線（通過した軌道） | 6/24/10t | 2.4 x12 | 15 | 3.3秒 |
| 4 | 通常 | **ゴム推進** | 自分が飛ぶ | 6/8/10t | 5 | 8 | 2.8秒 |
| 5 | ギア2 | **JETゴム銃** | 直線（通過した軌道） | 2/5/5t | 9 | 7 | 0.7秒 |
| 6 | ギア2 | **JETゴム乱打** | 直線（通過した軌道） | 4/26/10t | 2.9 x18 | 21 | 3.8秒 |
| 7 | ギア2 | **火拳銃** | 直線（通過した軌道） | 8/6/12t | 14 | 14 | 2.9秒 |
| 8 | ギア2 | **高速回避** | 自己強化 | 1/6/4t | — | 5 | 1.9秒 |
| 9 | ギア3 | **巨人の銃** | 直線（通過した軌道） | 14/8/18t | 18 | 18 | 3.4秒 |
| 10 | ギア3 | **巨人の斧** | 叩きつけ（着弾地点の周囲） | 18/8/22t | 22 | 22 | 4.4秒 |
| 11 | ギア3 | **巨人のバズーカ** | 前方の円錐 | 16/8/20t | 24 | 26 | 4.8秒 |
| 12 | ギア3 | **象銃** | 直線（通過した軌道） | 16/8/20t | 26 | 24 | 4.6秒 |
| 13 | ギア4・バウンドマン | **猿王銃** | 直線（通過した軌道） | 12/6/16t | 24 | 22 | 3.9秒 |
| 14 | ギア4・バウンドマン | **犀榴弾砲** | 自分が飛ぶ | 10/10/14t | 20 | 20 | 3.6秒 |
| 15 | ギア4・バウンドマン | **大猿王銃** | 直線（通過した軌道） | 26/10/30t | 42 | 40 | 10.0秒 |
| 16 | ギア4・スネイクマン | **JET大蛇砲** | 曲がる軌道 | 5/8/10t | 15 | 16 | 1.9秒 |
| 17 | ギア4・スネイクマン | **黒い蛇群** | 曲がる軌道 | 8/30/16t | 6 x8 | 30 | 5.9秒 |
| 18 | ギア4・スネイクマン | **王蛇** | 曲がる軌道 | 22/10/26t | 40 | 38 | 8.8秒 |
| 19 | ギア5・ニカ | **巨人化** | 自己強化 | 20/400/10t | — | 28 | 14.0秒 |
| 20 | ギア5・ニカ | **雷の投擲** | 飛翔体 | 12/20/14t | 22 | 24 | 4.4秒 |
| 21 | ギア5・ニカ | **地面のゴム化** | 足元の区域 | 14/200/12t | 2 | 26 | 7.0秒 |
| 22 | ギア5・ニカ | **地中からの打撃** | 遅延して打ち上げ | 16/30/14t | 20 | 22 | 4.9秒 |
| 23 | ギア5・ニカ | **白い星銃** | 全方位 | 18/10/20t | 26 | 34 | 7.8秒 |
| 24 | ギア5・ニカ | **猿神銃** | 直線（通過した軌道） | 40/14/40t | 70 | 58 | 28.0秒 |

技名は企画書の候補名です。「原作全技収録」ではありません。

---

## 看板演出 — ニカ変身後の浮遊・大笑い

変身の達成感を最も強く見せる場面として、専用に 4.5 秒を設計しています
（企画書 §08）。

| 時間 | 身体・表情 | VFX・音 |
|---|---|---|
| 0.00〜0.60秒 | 息を吸い、身体を少し丸める | heart_thud |
| 0.60〜1.20秒 | 胸・肩・頭の順に持ち上がる | cloud_curl |
| 1.20〜1.80秒 | 髪・眉・衣装・雲へ切替。笑顔から大笑いへ | cloud_curl、nika_rim |
| 1.80〜2.40秒 | 見た目を浮かせ、膝を上げて背を反らす | wind_ring |
| 2.40〜3.80秒 | 腹や顔へ手を添え、口を開き大爆笑 | cloud_orbit（声は入れない） |
| 3.80〜4.50秒 | 身体を戻し、軽く着地して専用待機へ | cloud_drift、wind_ring |

- 浮遊は**見た目だけ**です。プレイヤーの実座標は動かさないので、落下や壁抜けは
  増えません（見た目の root を約 0.62 ブロック上げています）。
- **声は入れません。** 許諾の確認できない音声を同梱しないという方針のため、
  打音と身体の動きだけで成立させています（企画書 §11）。
- 強い点滅は使いません。
- 設定の「演出短縮」で 1.7 秒の短縮版に切り替えられます。
- 被弾・死亡・再接続・ディメンション移動では演出を打ち切り、通常状態へ戻します。

---

## システム

### 気力

最大 100。技のコスト、形態の維持コスト、回復待ちを別々の項目にしています。

- 通常時の回復 1.2/秒、戦闘していない時間が続くと 2.4/秒
- **気力無限**（設定）は**消費だけ**を無効にします。クールダウン、被ダメージ、
  解放条件は無効になりません（企画書 §09 / QA-09）

### 品質設定

重いときに見栄えを丸ごと失うのではなく、重要な輪郭と命中表現から残します。
VFX は5層（予兆・軌道・接触・広がり・余韻）に分けてあり、削るのは外側からです。

| 項目 | 軽量 | 標準 | 高品質 |
|---|---|---|---|
| 描く層 | 予兆・軌道・接触 | 予兆・軌道・接触・広がり | 予兆・軌道・接触・広がり・余韻 |
| 粒子の密度 | 45% | 80% | 100% |
| 演出補助エンティティ | 8 | 16 | 24 |
| 大技のフル演出・同時数 | 1 | 2 | 2 |
| 本体キューブの設計予算 | 120〜220 | 250〜450 | 500〜1000 |

**接触（3層）はどの設定でも必ず描きます** — 命中が読めなくなるのが一番困るからです。

### ホスト設定

- **地形破壊**: 初期値 OFF
- **PvP**: 初期値 OFF（OFF の間、技は他プレイヤーを対象にしません）

### 全方位で判定する技

企画書 §09 の「単に自分の周囲を毎回全方向攻撃しない」を守るため、自分を中心に
全方位で判定してよい技は `tools/gla/spec.py` の `ALL_AROUND` に列挙した
**白い星銃**（宣言どおりの全方位）と**地面のゴム化**（足元の区域）だけです。
それ以外は前方・軌道・着弾地点のいずれかで判定します。この2つ以外に
`sphere` / `zone` を足すと、spec の自己検査が落ちます。

どちらもログポースの「ホスト設定」から変更します。

---

## 訓練場

ログポースの「訓練場」から、その場に港島を建てます。区画は企画書 §12 のとおり。

| 区画 | 目的 | 置いてあるもの |
|---|---|---|
| **訓練広場** | 標準距離で技を確認する | 距離目盛り・単体/群れの標的・壁越し判定用の遮蔽物 |
| **撮影エリア** | 形態とアニメの比較 | 昼夜を揃えた背景・正面と斜めの基準位置・全身が入る床目印 |
| **負荷テスト区画** | 同時戦闘時の重さを測る | 人数と敵数を固定できる配置・連続発動する技の指定 |
| **移動コース** | 見た目の追従を確認 | 段差・狭い入口・坂・ジャンプ・水際 |

「標的を片付ける」は**標的だけ**を消します。一度置いた地形は消しません。

---

## 開発者向け

### ビルド

```bash
./build.sh          # 2つのアドオンを両方ビルドして dist/ へ
```

必要なもの: Python 3.11+、`Pillow`、Node.js（スクリプトの読み込み確認と挙動テスト用）。

### 単一の定義元

**技・形態・アイテム・エンティティ・演出・QA表は、すべて
`tools/gla/spec.py` の1枚から生成されます**（企画書 §09）。
技を1つ増やすときに触るのはこのファイルだけです。そこから

| 生成物 | 生成元 |
|---|---|
| `packs/gla_RP/models/` `textures/entity/` | `tools/gla/gen_models.py` |
| `packs/gla_RP/animations/` `animation_controllers/` | `tools/gla/gen_anim.py` |
| `packs/gla_RP/particles/` `textures/particle/` | `tools/gla/gen_particles.py` |
| `packs/gla_RP/entity/` `attachables/` `render_controllers/` `textures/items/` | `tools/gla/gen_client.py` |
| `packs/gla_BP/items/` `entities/` `recipes/` `loot_tables/` `manifest.json` | `tools/gla/gen_bp.py` |
| `packs/gla_BP/scripts/data.js` | `tools/gla/gen_data.py` |
| `packs/gla_*/texts/` | `tools/gla/gen_lang.py` |
| この説明書 | `tools/gla/gen_docs.py` |

`packs/` 以下の生成物と `scripts/data.js` は**手で編集しないでください**。

### 手で書いてあるもの

`packs/gla_BP/scripts/` の `main.js` `state.js` `skills.js` `combat.js`
`fx.js` `ui.js` `training.js` `util.js` は手書きです（`data.js` だけが生成物）。

### 層の分け方（企画書 §13）

```
入力        main.js       アイテム使用・技切替・形態選択を1つに正規化
ゲーム状態  state.js      form / phase / 気力 / 解放状態 / 復旧
戦闘処理    combat.js     判定の形・命中履歴(action_id)・ノックバック
技          skills.js     溜め→有効→後隙 の進行と演出の予約
表示        fx.js         5層の並べ方と品質による間引き
UI          ui.js         形態選択・設定・技一覧
訓練場      training.js   港島の建築
```

状態遷移は `normal → transforming → active → attacking → recovering → active`、
解除は `reverting → normal`、異常時は `safe_reset → normal`。

### ブラインド批評用のカード

```bash
python3 tools/gla/blind_sheet.py
```

技名を伏せたまま、24技の時間配分・判定の形・命中回数・反動を1枚に並べた
`out/blind_techniques.png` と、答え合わせ用の `out/blind_answers.md` を作ります
（企画書 §10 差別化の検収 / §16 ブラインド批評）。

カードだけを見て「伸ばす技」「連打」「重い一撃」を言い当てられるかを試し、
言い当てられない組があれば `spec.py` の windup / active / recover / hits /
knockback を離します。**設計の段階で見分けが付かないものは、実機でも
見分けが付きません。**

### 基準版の記録

`./build.sh` は `dist/RELEASE_RECORD.md` も書き出します（企画書 §18 基準版の記録）。
配布ファイル名、パックの中身のハッシュ、BP/RP の UUID と版、本体版と
モジュール版が入っています。ハッシュは zip の時刻ではなく**中身**から取るので、
同じ内容なら何度ビルドしても同じ値になります。

実機名・設定・確認日・確認者・合格した QA 項目の欄は**空のまま**です。
実機で確認した人がそこへ記入するまで、確認は行われていないことを意味します。

### 検証コマンド

```bash
python3 tools/validate.py         # 参照欠落・UUID・依存・言語キー（両アドオン）
python3 tools/check_scripts.py    # スクリプトが実際に読み込めるか（両アドオン）
python3 tools/gla/test_logic.py   # 下の QA 表の「自動」項目を実行
```

### 開発用コマンド

製品の進行（解放条件・レシピ）とは分けてあります（企画書 §12）。

| コマンド | 効果 |
|---|---|
| `/scriptevent gla:power` | 能力を得る |
| `/scriptevent gla:unlock` | 全形態を解放する |
| `/scriptevent gla:form gear5` | 指定の形態へ変身する |
| `/scriptevent gla:recover` | 通常状態へ復旧する |
| `/scriptevent gla:reset` | 進行をすべて初期化する |

---

## QA・検収チェックリスト（企画書 §17）

「入る」「見える」「動く」「戻れる」を分けて確認します。
**自動検査で潰せるのは JSON・参照・数値・命中ロジックまでで、
見た目・音・タッチ操作・負荷は実機でしか検収できません。**

| ID | 試験 | 合格条件 | 現状 |
|---|---|---|---|
| QA-01 | 新規インポート | BP/RPが認識され、依存不足・参照欠落・致命的エラーがない。 | 自動: tools/validate.py（参照欠落・UUID・依存・アイコン） |
| QA-02 | 更新導入 | 既存版から更新し、保存した設定・解放状態を保つ。 | **実機未検証** |
| QA-03 | アイテム使用100回 | 変身・技切替・設定操作で個数の増減がない。 | 自動: tools/gla/test_logic.py（100回操作で個数不変） |
| QA-04 | 変身・解除20往復 | 本体消失、二重表示、旧形態残留、予約技の暴発がない。 | 自動: tools/gla/test_logic.py（20往復で残留なし） |
| QA-05 | 全身の可視性 | 前後の三人称、昼夜、屋内で形態と表情が判別できる。 | **実機未検証** |
| QA-06 | 最大伸長・巨大拳 | 描画範囲外への早すぎる消失、関節の穴、UV破綻がない。 | **実機未検証** |
| QA-07 | 単発と連打の命中 | 定義回数だけ命中し、背面・壁越し・自分へ誤爆しない。 | 自動: tools/gla/test_logic.py（単発1回／連打は定義回数／壁越し・自分に当たらない） |
| QA-08 | 命中タイミング | 接触の見た目と判定時刻の差を原則1tick以内へ。 | **実機未検証** |
| QA-09 | 気力無限 | 消費だけ無効。無敵・連射無制限が生じない。 | 自動: tools/gla/test_logic.py（消費だけ無効・クールダウンは有効） |
| QA-10 | 地形破壊OFF | すべての大技で地形変更と不要な火災を起こさない。 | 自動: tools/gla/test_logic.py（全24技でブロック書き換え0） |
| QA-11 | 死亡・再参加 | 復旧し、操作制限と視点が残らない。 | **実機未検証** |
| QA-12 | 場所の切替 | 水中、段差、ディメンション移動後も追従と復旧が成立。 | **実機未検証** |
| QA-13 | 2〜4人同時 | 他人の形態・技・カメラが混ざらない。 | **実機未検証** |
| QA-14 | 30分の連続戦闘 | 劣化、残留エンティティの増加、クラッシュがない。 | **実機未検証** |
| QA-15 | タッチ操作・UI | 文字が切れず、技切替と発動を誤って同時受付しない。 | **実機未検証** |

---

## 企画書との対応

### この版に入っているもの

- 形態専用の立ち姿・移動、表情差分、変身・解除、攻撃ごとの当たり判定
- 技選択 UI、軽量化設定、訓練場、再接続時の復旧
- 配布用 BP/RP と、その検証手順（上の検証コマンド）

### 意図的に入れていないもの

| 項目 | 理由 |
|---|---|
| カメラの寄り・切替演出 | Camera API の対応版を実機で確認するまで入れない（企画書 §11 / §14）。カメラの揺れのみ、設定で OFF にできる形で実装 |
| 地形を実際に壊す処理 | ホストの検証済みルールが決まるまで、許可されても実行しない（QA-10 を壊さないため） |
| 一人称の専用腕 | 三人称の完成後、別検収で追加する工程（企画書 §14） |
| 全キャラクター | 第1弾はルフィのみ。品質基準の完成後に追加（企画書 §03） |
| 自作の効果音 | 許諾の確認できる音だけを同梱する方針のため、現状はバニラの音のみ |
| 左右移動クリップの自動再生 | クリップは生成・宣言済みだが、アタッチャブルが読める組み込みクエリでは「横に動いている」を出し分けられないため、自動では鳴らしていない |
| 敵の配置と報酬 | 企画書 §12 の「サバイバル導線の追加順」どおり、訓練モードの完成後の工程。現状あるのは訓練用の標的だけ |

### 身体の動きを誰が鳴らすか

変身中に見えている身体はアタッチャブルなので、そこで再生できるのは
`controller.animation.gla.form` が名指ししたクリップだけです。

| 動き | 誰が鳴らすか | 版への依存 |
|---|---|---|
| 待機・歩行・走行・空中・着地・しゃがみ | コントローラが組み込みのクエリで出し分ける | なし |
| 登場ポーズ / **ニカの看板演出（§08）** | コントローラの `initial_state`。表示体は形態アイテムを装備した瞬間に作られるので、変身のたびに必ず1回再生される | なし |
| 24技それぞれの専用クリップ | スクリプトの `player.playAnimation()`（best effort） | **未検証** |
| 左右移動・被弾 | 同上（コントローラからは「横に動いている」「今殴られた」を観測できない） | **未検証** |

看板演出は、目玉の演出をスクリプト任せにしないため、コントローラ側に
持たせてあります。演出中にプレイヤーが動き出せばその場で移動へ抜けるので、
設定の「演出短縮」でスクリプトが先に操作を返したときも自然に短くなります。

この作りの副作用として、**変身したまま再接続すると登場演出がもう一度
再生されます**（表示体が作り直されるため）。動き出せばすぐ抜けます。

技ごとの専用クリップが対象版で鳴らなかった場合でも、コントローラ側の
移動・待機と、層に分けた VFX で技は成立するよう作ってあります。

---

## ライセンスと権利について

- 本アドオンは**非公式のファン制作**です。ONE PIECE および Minecraft の
  権利者とは関係がありません。
- モデル・テクスチャ・パーティクル・アニメーションは**すべてこのリポジトリの
  スクリプトが生成した自作物**です。原作映像からの抽出物は含みません。
- 効果音はバニラ Minecraft の音のみを参照しています。音声ファイルは同梱していません。
- 公開・再配布の際は、非公式である旨の表示を外さないでください。
  Minecraft 側の利用ガイドラインと、ONE PIECE 側の権利・素材利用条件は
  別々に確認が必要です。非営利・自作であることだけを理由に、
  公開許諾済みとは扱えません（企画書 §18）。
